name: deploy
on:
  push:
    branches: master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: np-complete/build-book-action@v2
      - uses: np-complete/puiblish-book-action@v2
